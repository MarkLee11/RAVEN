===============================================================================
RAVEN 安全基线与依赖治理（Phase 7）
===============================================================================
日期：2026-09-20

一、执行项
-------------------------------------------------------------------------------
1) 依赖漏洞扫描
- 命令：`npm audit --json`
- 初始结果：28 项（low 3 / moderate 7 / high 17 / critical 1）

2) 非破坏性修复
- 命令：`npm audit fix`
- 修复后结果：9 项（low 2 / moderate 4 / high 2 / critical 1）

3) 回归验证
- `npm run lint` ✅
- `npm run typecheck` ✅
- `npm run test` ✅
- `npm run build` ✅
- `npm run test:e2e` ✅

二、风险解读
-------------------------------------------------------------------------------
剩余高危/严重项主要来自：
- 工具链依赖链（vite/vitest/esbuild）与安全公告窗口期
- 若执行 `npm audit fix --force` 将触发主版本升级（存在兼容风险）

三、策略决策
-------------------------------------------------------------------------------
- 当前采用“稳定优先”策略：
  - 先执行非破坏性修复并通过全量回归；
  - 对需要 major 升级的依赖单独建升级分支，做兼容测试后再合并。

四、关键安全检查点
-------------------------------------------------------------------------------
- 前端仅使用 `VITE_SUPABASE_ANON_KEY`（未发现将 service_role key 暴露到前端代码）
- `SUPABASE_SERVICE_KEY` 仅出现在脚本/运维场景（非前端运行时）

五、后续行动
-------------------------------------------------------------------------------
- 建立每周一次 `npm audit` 基线检查；
- 为 vite/vitest 升级建立专项任务，评估到最新安全版本。

===============================================================================
